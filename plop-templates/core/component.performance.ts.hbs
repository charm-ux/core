import { html, testBundleSize, testRenderTime } from 'web-test-runner-performance/browser.js';
import { expect } from '@open-wc/testing';
import './index.js';
import type { Core{{pascalCase name}} } from './index.js';

describe('{{kebabCase name}} performance', () => {
  const element = html`<ch-{{kebabCase name}}>TODO: change this</ch-{{kebabCase name}}>`;

  it(`should render under 20ms`, async () => {
    expect((await testRenderTime(element)).duration).to.be.lessThan(20);
  });
  it('should have a small bundle', async () => {
    expect((await testBundleSize('./dist/components/{{kebabCase name}}/{{kebabCase name}}.js')).kb).to.below(1);
  });
});
