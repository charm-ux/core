---
'@charm-ux/core': minor
---

Improve the icon component by safely parsing external SVG content, deduplicating concurrent fetches, differentiating retryable vs cacheable failures, and adding `rotate` and `flip` support.
