---
'@charm-ux/core': minor
---

Add `resize="auto"` auto-grow mode to `ch-text-area`

The textarea grows to fit its content as the user types and hides its scrollbar,
recomputing its height whenever the textarea's width changes and text re-wraps.
