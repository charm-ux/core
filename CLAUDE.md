# Charm UX

Read [AGENTS.md](AGENTS.md) for the project overview, conventions, and how the design-system
rules are organized. The full rule set lives in [`.agents/rules/`](.agents/rules/README.md).

Claude Code path-gated rules are configured in [`.claude/rules/`](.claude/rules/) — each file
declares `globs` and condenses a rule category, linking back to the detailed rule files in
`.agents/rules/`.
